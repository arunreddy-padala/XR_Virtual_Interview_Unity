using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WandDetectionComponent : MonoBehaviour
{
    // all object needs to this trigger object for color change
    
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
